# Digital_Hunter
I have three services here, each one receiving from one topic and doing the actions it needs to do. I added another DB file that actually initializes the DB to load all the tables and then all the other services can insert information into it accordingly.

to run this project: docker-compose up -d